/**
 * This module creates and draws the grids for the zones in the map.
 */
 
module.exports = function ZoneManager(zones) {

	/**
	 * Module imports.
	 */
	var GeoZone = require('./geoZone.js');
	
	var zoneList;
	var colors;
	
	/**
	 * Gets the color for a risk level classification.
	 *
	 * @param level: (int) the risk level classification
	 * @return String: the level's color
	 */
	this.getLevelColor = function(level) {

	};
	
	
	/**
	 * Creates a rectangle object using the zone's coordinates
	 * and then draws each rectangle in the map.
	 *
	 */
	this.drawZonesGrid = function() {

	};
	
};